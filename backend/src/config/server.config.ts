import { NestApplicationContextOptions } from "@nestjs/common/interfaces/nest-application-context-options.interface";

export const ServerConfig: NestApplicationContextOptions = {
    bufferLogs: true
}